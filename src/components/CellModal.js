import React from 'react';

import Box from '@mui/material/Box';
import Modal from '@mui/material/Modal';
import Button from '@mui/material/Button';

const CellModal = ({ open }) => {

  return (
    <div>

    </div>
  )
}


export default CellModal;